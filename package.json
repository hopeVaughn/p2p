{
  "devDependencies": {
    "turbo": "^1.10.15"
  },
  "scripts": {
    "dev": "turbo run dev --no-daemon",
    "build": "turbo run build",
    "start": "node apps/api/dist/src/main"
  },
  "workspaces": [
    "apps/*"
  ]
}
