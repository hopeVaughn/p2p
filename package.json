{
  "devDependencies": {
    "turbo": "^1.10.9"
  },
  "scripts": {
    "dev": "turbo run dev --no-daemon",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ]
}
