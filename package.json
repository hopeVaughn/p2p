{
  "devDependencies": {
    "turbo": "^1.10.13"
  },
  "scripts": {
    "dev": "turbo run dev --no-daemon",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ]
}
