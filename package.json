{
  "devDependencies": {
    "turbo": "^1.10.16"
  },
  "scripts": {
    "dev": "turbo run dev --no-daemon",
    "build": "turbo run build",
    "start": "node apps/api/dist/src/main"
  },
  "workspaces": [
    "apps/*"
  ]
}
